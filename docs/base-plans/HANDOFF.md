# HANDOFF — точка входа для новой сессии

Документ для быстрого входа в контекст: что делаем, что читать, чему в
репозитории **не верить**, и в каком состоянии окружение.

- **Обновлён:** 2026-08-16
- **Ветка:** `claude/s0-roadmap-mvp-handoff-b2feb0` (git worktree)

---

## 1. Что делаем

Реализуем первый сквозной срез из [Roadmap-MVP.md](Roadmap-MVP.md), шаги
`S0`→`S7`. Цель среза — не функциональность, а ответ на один вопрос:

> Существует ли длина поводка генератора, при которой стиль уже читается как
> аниме, а персонаж ещё узнаётся как тот же самый между ракурсами.

Это гейт `S3`. Всё остальное в срезе — обвязка, чтобы до этого вопроса дойти.

## 2. Порядок чтения

| # | Документ | Зачем |
| - | -------- | ----- |
| 1 | [Roadmap-MVP.md](Roadmap-MVP.md) | Что строим сейчас: состав, матрица, шаги, раскладка кода. |
| 2 | [Roadmap.md](Roadmap.md), разделы 7, 13, 17 | Решения по стилю и стеку; критический разбор «вычисления против консистентности». |
| 3 | [ADR 0002](../architects/adr/0002-style-comes-from-project-assets.md) | Граница ответственности: содержание наше, облик пользовательский. |

Полная картина решений — [Roadmap.md](Roadmap.md) целиком, но для начала работы
достаточно трёх пунктов выше.

## 3. Чему в репозитории НЕ верить

Документация переразобрана: всё, что описывало снятую парадигму, вынесено в
[docs/archive/](../archive/README.md) целиком. Актуальные разделы можно читать
буквально, без оговорок. **Если документ из `archive/` противоречит любому
другому — прав второй.**

Осталось одно исключение, которое живёт в коде и потому в архив не переносится:

> **`engine/worldgen/dna/` — не ядро.** Каркас ДНК-компрессора написан до смены
> парадигмы и понижен до кода retrieval-ключа
> ([Roadmap 11.5](Roadmap.md#11-что-устарело-в-старом-плане)). В текущем срезе не
> используется. Не принимать за основу и не развивать, пока не понадобится
> реестр ассетов.

## 4. Ключевые решения, которые нельзя размыть

- **Blender** headless под MVP; **Godot** отложен с явным триггером возврата
  (пропускная способность рендера либо интерактивное превью). Чтобы возврат
  остался дешёвым: **сцена описывается данными, рендерер их потребляет**, логики
  сцены в рендер-скрипте нет.
- **Физика в срез не входит.** Она дорогая и отвечает за другую ось
  ([Roadmap 17.2](Roadmap.md#172-физика--самый-дорогой-компонент-не-решающий-нашу-задачу)).
  Для изображений «упал» — это поза, «разрушен» — вариант ассета.
- **Одна стилевая плоскость** (cel/аниме). Разнообразие уходит внутрь неё — в
  шкалу длины поводка, а не в набор стилей.
- **Удешевление и консистентность направлены в одну сторону**
  ([Roadmap 17.1](Roadmap.md#171-главный-вывод-дорогое-не-решает-консистентность)):
  короче поводок — и дешевле, и надёжнее. Это основной вектор оптимизации.
- **Тонкий срез целиком, потом утолщение.** Не улучшать отдельный слой, пока
  цепочка не работает от начала до конца.

## 5. Состояние окружения

| Что | Состояние |
| --- | --------- |
| `engine/.venv` | **готов**: torch 2.13.0+cpu, hydra, mlflow, pytest, ruff, mypy |
| Тесты | 77/77 проходят, `ruff` чист |
| `mypy` | **5 ошибок** в старом коде (`tests/test_dna_dataset.py`, `tests/test_tracking.py`, `scripts/train_dna.py`, `scripts/reproduce.py`) — были до среза MVP, новый код чист |
| **Blender** | **5.1.1**, Steam-установка: `D:\SteamLibrary\steamapps\common\Blender\blender.exe`. Находится автоматически, переопределяется `WORLDGEN_BLENDER` |
| **Ассет героя** | **нет файла**. Решение: VRoid Studio → VRM в `engine/data/assets/hero.glb` (см. [data/assets/README](../../engine/data/assets/README.md)) |
| `gh` CLI | недоступен, PR из сессии не создать |
| Python | 3.12 (Windows); внутри Blender — свой 3.13 |

Команды:

```bash
cd engine
.venv/Scripts/python -m pytest tests/ -q
.venv/Scripts/ruff check .
.venv/Scripts/python scripts/render_scene.py --scene configs/mvp/scene.proxy.json --all-cameras
```

**Про Blender 5.x.** API отличается от 4.x в двух местах, на которые легко
наступить: идентификатор движка — `BLENDER_EEVEE` (не `BLENDER_EEVEE_NEXT`), а
multilayer EXR требует сперва `image_settings.media_type = "MULTI_LAYER_IMAGE"` и
только потом `file_format = "OPEN_EXR_MULTILAYER"`.

**Проверено на живом бинаре:** EEVEE headless отдаёт в один multilayer EXR
`Combined`, `Depth.Z`, `Normal.XYZ` и `CryptoObject00/01/02` с манифестом имён
объектов. Премиса S1 («Cryptomatte закрывает вопрос чистых масок») подтверждена в
части наличия пассов; чистота масок на границах — проверка самого S1.

**Git.** Работа идёт в worktree на ветке `claude/claude-config-files-0da536`.
`main` занят основным worktree, поэтому слияние делается через временную ветку:

```bash
git fetch origin main && git branch merge-main origin/main && git checkout merge-main && git merge --no-edit claude/claude-config-files-0da536 && git push origin merge-main:main && git checkout claude/claude-config-files-0da536 && git merge --ff-only merge-main && git push && git branch -d merge-main
```

**Известная неприятность:** git иногда подхватывает Windows-версию `ssh`
(`C:\WINDOWS\System32\OpenSSH`), которая ключ не принимает, тогда как
`/usr/bin/ssh` из Git Bash аутентифицируется нормально. Проявляется как
`Permission denied (publickey)` при флапающем соединении. Лечится повтором;
проверка — `ssh -T git@github.com`.

## 6. Первые действия

Blender установлен, каркас `S0` собран и работает на болванке. Осталось:

1. **Положить героя в `engine/data/assets/hero.glb`.** Решение принято: VRoid
   Studio → экспорт VRM. Требование, которое легко упустить: персонаж должен быть
   стилизованным, но **геометрически честным** — уровня игрового или
   VTuber-персонажа. Модели, воспроизводящие нарисованное аниме-лицо (глаза
   одного размера с любого ракурса, причёска не в перспективе), в срез не
   годятся: они не сводятся между ракурсами, и мы провалимся не на том, что
   проверяем. Подробности и соглашение об осях —
   [data/assets/README](../../engine/data/assets/README.md).
2. **Закрыть `S0` на настоящем ассете:** прогнать три ракурса, поправить масштаб,
   разворот и свет в [`configs/mvp/scene.json`](../../engine/configs/mvp/scene.json),
   удалить болванку вместе с `scene.proxy.json`.
3. **`S1`** — cel-шейдинг, контуры и вывод пассов в multilayer EXR поверх готового
   каркаса `worldgen/render/`.

`.vrm` пробуется штатным импортёром glTF (расширения VRM он игнорирует, геометрию
и скелет берёт). Если конкретная модель не зайдёт — понадобится VRM-аддон, и тогда
запуск должен идти без `--factory-startup` (параметр у `run_blender_script`).

## 7. Что фиксировать по ходу

- Гейт `S3` (есть окно поводка или нет) — **обязательно** в
  [decision-log.md](decision-log.md), с числами.
- Любой переход `🔴` → `🟡` → `✅` — в [Roadmap.md](Roadmap.md).
- Значимое архитектурное решение — отдельным [ADR](../architects/adr/).
- Статусы шагов `S0`–`S7` — в самом [Roadmap-MVP.md](Roadmap-MVP.md).
